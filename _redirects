# ================================================================
# cuisine-sans-pfas.fr — Netlify _redirects
# SPA fallback: all routes serve index.html with 200 (not 301)
# This allows the JS router to handle /poele-inox, /poele-ceramique etc.
# ================================================================

# SPA fallback — must be LAST rule
/*    /index.html    200
